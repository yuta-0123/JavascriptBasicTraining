// arg: 高さと底辺の値
// logic: 面積を計算する
// return: 面積

function getArea(base,height) {
  let base = 10;
  let height = 5;
  var triangle=1/2*height*base;
  return triangle
}

// オブジェクトのインスタンス化
// 利用したいオブジェクトの中のメソッド
// 関数を制作(引数,処理,戻す)

// I => month
// O => season

function getSeason(month){
  if(month<3){
    return "春"
  }
}
